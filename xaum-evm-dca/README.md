# XAUM DCA

Smart contracts for dollar-cost averaging (DCA) into XAUm on EVM chains. Users deposit stablecoins (or yield-bearing tokens such as aUSDT) and the system periodically converts them into XAUm through whitelisted swap adapters, with timelocked upgrades and admin operations for security.
